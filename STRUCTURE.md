# Estructura del proyecto (orden de carpetas)

Este repo contiene 2 partes:
- **Flutter app** (raíz del repo)
- **WEBNIDA** (web: backend Laravel + frontend Next + deploy)

## Árbol (orden sugerido)

APPNID/
  .dart_tool/ (generado — NO se sube)
  android/
  assets/
  build/ (generado — NO se sube)
  ios/
  lib/
  linux/
  macos/
  test/
  web/
  WEBNIDA/
    backend/
    deploy/
    docs/
    frontend/
    node_modules/ (generado — NO se sube)
    tools/
    bootstrap-laravel.ps1
    build-deploy.ps1
    clean.ps1
    COMO-EJECUTAR.md
    deploy_new.zip
    iniciar.ps1
    package.json
    README.md
    run-all.ps1
    run-backend.ps1
    run-frontend.ps1
    setup-php-composer.ps1
  windows/
  .flutter-plugins-dependencies (generado)
  .gitattributes
  .gitignore
  .metadata
  analysis_options.yaml
  devtools_options.yaml
  flutter_application_1.iml
  GUIA_MOVIL_SEGUIMIENTO.md
  package-lock.json
  pubspec.lock
  pubspec.yaml
  README.md
  zzz.txt
